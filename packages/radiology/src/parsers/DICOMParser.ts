import { VectorDiffAnimation } from '@vectordiff/core';
import * as dicomParser from 'dicom-parser';

export class DICOMParser {
  // Placeholder
}

export function parseDICOMSeries(dicomFiles: ArrayBuffer[]): VectorDiffAnimation | null {
  console.log('Parsing DICOM series...');
  // Właściwa implementacja wymagałaby iteracji przez pliki
  // i tworzenia obiektów VectorDiff
  return null;
}
