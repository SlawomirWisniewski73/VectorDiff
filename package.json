{
  "name": "vectordiff-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for the VectorDiff project",
  "private": true,
  "workspaces": [
    "packages/*",
    "applications/*"
  ],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "test": "npm run test -ws --if-present"
  },
  "engines": {
    "npm": ">=8.5.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
