{
  "name": "vectordiff-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "core",
    "visualization",
    "packages/*",
    "applications/*"
  ],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "test": "npm run test -ws --if-present"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
